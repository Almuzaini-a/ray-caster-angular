import { Routes } from '@angular/router';
import { RaycasterComponent } from './components/raycaster/raycaster.component';

export const routes: Routes = [
    { path: '', component: RaycasterComponent }
];
